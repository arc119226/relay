{
  "name": "relay",
  "version": "0.1.0",
  "private": true,
  "description": "NIP-01 子集的 WebSocket 訊令 relay,跑在 Cloudflare Durable Object 上。什麼都不存。",
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/arc119226/relay.git"
  },
  "bugs": {
    "url": "https://github.com/arc119226/relay/issues"
  },
  "homepage": "https://github.com/arc119226/relay#readme",
  "author": "Arc Liu",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "check": "pnpm lint && pnpm typecheck && pnpm test",
    "deploy:check": "wrangler deploy --dry-run",
    "deploy": "wrangler deploy",
    "smoke:build": "esbuild tools/trystero-smoke/entry.mjs --bundle --format=esm --platform=browser --outfile=tools/trystero-smoke/dist/bundle.js"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "0.12.21",
    "@cloudflare/workers-types": "^4.20250620.0",
    "esbuild": "^0.28.2",
    "eslint": "^9.39.5",
    "trystero": "0.25.3",
    "typescript": "^5.7.0",
    "typescript-eslint": "^8.69.0",
    "vitest": "^3.0.0",
    "wrangler": "^4.0.0"
  },
  "packageManager": "pnpm@11.9.0"
}
