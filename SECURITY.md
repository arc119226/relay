# 安全性回報

## 怎麼回報

請走 GitHub 的 **Private vulnerability reporting**（本 repo 的 Security 分頁 →
Report a vulnerability）。不要開公開 issue。

這不是專職維護的專案，沒有 SLA 也沒有獎金——但只要收到就會處理。

## 這台東西在信任模型裡的位置

**它是別人眼中的第三方。** 一個訊令 relay 的工作就是幫兩個素不相識的端點牽線，
所以它天生站在使用者的信任邊界**之外**。這份文件的重點因此不是「我們很安全」，
而是**把營運者看得到什麼講清楚**，讓連上來的人自己判斷。

（作者自己的另一個專案 [accounting](https://github.com/arc119226/accounting)
的 SECURITY.md 有一節叫「第三方看得到什麼」——這台 relay 就是那個第三方。）

## 攻擊面

沒有帳號、沒有資料庫、沒有後端 API，所以一般 web 服務的多數類別不適用
（沒有 SQL 注入、沒有 session 可以劫持、沒有使用者資料可以外洩——因為沒有存）。
實際存在的面向有三塊：

1. **wire 協定**（`src/nip01.ts`、`src/match.ts`）——輸入是任何人送來的 JSON，
   完全不可信。契約是**永不 throw**：形狀壞就回 `{ok:false, reason}`，
   一個壞客戶端不該影響同一顆 DO 上的其他人。能繞過驗形把畸形資料送進
   `matches()` 的話，那是真的問題。
2. **資源耗盡**——單一 Durable Object 是單執行緒的，而扇出是 O(socket × 訂閱)。
   對抗式覆核抓到過兩條真的路徑（一則事件放大成 4000 次序列化、200 條閒置連線
   免費鎖死所有人），現在的閘門列在下面。
3. **連線耗盡**——沒有認證，任何人都連得上。

### 現有的閘門

| 閘 | 值 | 擋什麼 |
| --- | --- | --- |
| 訊框大小（在 `JSON.parse` **之前**） | 16384 code unit | 不花令牌就逼 DO 解析大 JSON |
| 每條連線的令牌桶 | 容量 40、20/秒 | 單一客戶端洗版 |
| **整顆 DO 的事件桶** | 爆發 200、50/秒 | 很多條各自「合規」的連線一起灌 |
| 每個 IP 的同時連線 | 8 | 閒置連線佔滿名額 |
| 閒置回收 | 5 分鐘 | 半開的 socket 永久佔住名額 |
| 每條連線的訂閱數 / 每個 filter 的主題數 | 20 / 16 | 放大扇出、撐爆 attachment |
| 同時連線總數 | 200 | 兜底 |

數字與理由都在 `src/limits.ts` 的註解裡，全部是量出來的，不是估的。

## 不算漏洞的

下面這些是**刻意的決定**，理由寫在 `docs/spec.md`。回報之前請先讀：

- **不驗 schnorr 簽章。** 客戶端（Trystero）的金鑰是 module scope 的
  `schnorr.keygen()`——**每次開網頁一組新的**，所以 `pubkey` 不是身分而是亂數。
  驗簽章擋不到任何人（攻擊者產一組新的就好，成本是零），也做不了白名單
  （合法使用者的 pubkey 也每次不一樣）。事件 `id` 的 SHA-256 **有**驗，
  那擋的不是攻擊，是格式錯誤的客戶端。詳見 spec §4。
- **知道 topic 就收得到那個 topic 的事件。** 那正是 relay 的工作。
  內容的機密性由客戶端負責（Trystero 以 appId + 房間碼派生金鑰加密 SDP），
  relay 從設計上就看不到明文。
- **沒有認證、沒有帳號。** 這是訊令 relay 不是應用伺服器。
- **每個 IP 8 條連線的上限可能誤傷大型 NAT。** 那是刻意的取捨：
  沒有這條，200 條零成本的閒置連線就能讓所有真客戶端永遠拿 503。
- **扇出包含發送者自己。** 標準 Nostr relay 的行為，客戶端據此設計。

## 營運者看得到什麼

任何人自己架一份都適用；`relay.arc.idv.tw` 由本專案作者營運。

**看得到：**

- 你的 **IP 位址**與**連線時間**
- 你訂閱與發布的 **topic 值**（在 `#x` 標籤裡）。Trystero 送來的是
  `SHA-1(appId + roomId)` 轉 base36 的字串，**不是房間碼本身**
- 事件的 `kind`、`created_at`、`pubkey`、`sig`、以及 **`content` 的密文**
- 有一段加密資料流過（訊令約 2 KB）

**看不到：**

- **`content` 的明文**——那是 WebRTC 的 SDP，由客戶端以房間碼派生的金鑰加密
- **房間碼本身**
- 配對成功之後的任何東西——那走兩端之間的 WebRTC 直連，完全不經過 relay

**存不存得下來：** 存不了。全檔零 `ctx.storage`、零 alarm、零事件陣列，
而且是 `test/storageFree.test.ts` 用**原始碼比對**守著的——
一旦有人為了加歷史功能而寫下第一個 `storage`，那條測試立刻紅。
所以「什麼都不存」在這條路徑上是**結構性事實**，不是自律。

但要誠實：**營運者換掉程式碼就能開始記錄。** 上面那條保證的是
「這份原始碼跑起來不存」，不是「那台機器不可能存」。不想被任何人看到連線
中繼資料的話，正確的做法是自己架一份（見 README 的「自己部署」），
而不是相信這份文件。

## 對抗式覆核

`docs/plan.md` 記著一次四面向的對抗式覆核：12 項發現、駁回 9 項、確認 3 項，
以及部署之後在正式站量到的第 4 項（半開連線永久佔住名額）。那些修正就是
上面那張閘門表的來歷。歡迎繼續找——尤其是資源耗盡那一類，
因為單執行緒的 DO 天生對放大很敏感。
